# REST naming

Plain notes with no frontmatter at all - the way many hand-written team skills look.

- Collection resources are plural nouns: `/orders`, never `/getOrders`.
- Path segments are kebab-case; query parameters are camelCase.
